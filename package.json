{
  "name": "fake-store-api-testing",
  "version": "1.0.0",
  "description": "Integration and performance tests for the Fake Store API",
  "scripts": {
    "db:seed": "cd fake-store-api && npm run seed",
    "cy:open": "npm run db:seed && cypress open",
    "cy:run": "npm run db:seed && cypress run",
    "k6:load": "k6 run k6/load-test.js",
    "k6:stress": "k6 run k6/stress-test.js"
  },
  "devDependencies": {
    "cypress": "^13.0.0"
  }
}
